# Movie Theatre Frontend

To run the frontend:
``` 
cd client
npm install
npm run dev
```

Note: should have the MySQL and backend Spring Boot server running at the same time (movie-theatre-system-backend)
